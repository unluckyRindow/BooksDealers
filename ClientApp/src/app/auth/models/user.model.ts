export interface RegistrationData {
    login: string;
    password: string;
    email: string;
    name: string;
}

export interface LoginData {
    login: string;
    password: string;
}

export interface UserData {
    id: string;
    name: string;
    email: string;
}
